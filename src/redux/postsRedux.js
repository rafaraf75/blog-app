// selectors (dodamy później)

// actions
const createActionName = actionName => `app/posts/${actionName}`;

// action creators (dodamy później)

// reducer
const postsReducer = (statePart = [], action) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default postsReducer;
